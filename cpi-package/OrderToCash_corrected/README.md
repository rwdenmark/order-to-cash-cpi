This XSLT replaces the deployed `../OrderToCash/src/main/resources/mapping/OrderToFulfillment.xsl` for Phase 4.
Paste it into the iFlow's XSLT mapping step, then Save as version and re-export the package.
